# Screenshots

Put the menu bar panel screenshots here so the README can show them:

- `session-health.png` — the **会话健康 / Session health** tab (hero + stats + current session + list)
- `proxy-status.png` — the **代理状态 / Proxy status** tab

Suggested width ~280–560pt (retina @2x is fine). PNG.
The README references them at `docs/screenshots/<name>.png`.
